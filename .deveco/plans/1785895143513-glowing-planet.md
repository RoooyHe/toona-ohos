# Plan: Delete MatrixHttpClient/MatrixConfig, Replace with DI

## Goal
- Delete MatrixHttpClient mega-facade and MatrixConfig singleton, replace with direct constructor injection of specific HTTP sub-clients into services

## Constraints & Preferences
- ArkTS rules: NO `any`/`unknown`, NO destructuring, NO spread, NO `as unknown as` double assertions, NO constructor param properties, explicit return types on async methods, explicit type context on object literals, `Record<string, string | object>` not `Record<string, object>` alone
- Must preserve CryptoService/VerificationService/EventProcessor wiring chain
- All files under `C:\Users\Roy\DevEcoStudioProjects\toona_ohos\toona\src\main\ets\`
- Do NOT run arkts_check or build — just write files

## Progress

### Done
- **Step 1**: Added `putTyping()`, `getTypingUsers()` to RoomHttpClient.ets
- **Step 2**: Made `getBaseUrl()` public in BaseHttpClient.ets
- **Step 2b**: Fixed MediaHttpClient.ets `this.accessToken` → `this.getAccessToken()`
- **Step 3**: Rewrote AuthManager.ets — takes `ConnectionConfig` + `BaseHttpClient`; removed `MatrixConfig`/`MatrixHttpClient` imports; removed `getHttpClient()`
- **Step 4**: Rewrote SyncManager.ets — takes `BaseHttpClient` as 5th constructor param
- **Step 5**: Rewrote RoomManager.ets — takes `RoomHttpClient` as 3rd constructor param; added `searchPublicRooms()` and `searchUsers()`
- **Step 6**: Rewrote MessageManager.ets — takes `MessageHttpClient` as 3rd constructor param
- **Step 7**: Rewrote all singleton services:
  - CryptoManager.ets — public constructor takes `DeviceHttpClient`
  - DeviceManager.ets — public constructor takes `IAuthManager` + `DeviceHttpClient`
  - FilterManager.ets — public constructor takes `FilterHttpClient`
  - MediaManager.ets — public constructor takes `MediaHttpClient`
  - OpenIdManager.ets — public constructor takes `OpenIdHttpClient`
  - PresenceManager.ets — public constructor takes `PresenceHttpClient`
  - PushManager.ets — public constructor takes `UserHttpClient`
  - RoomStateManager.ets — public constructor takes `RoomHttpClient`
  - RoomTagManager.ets — public constructor takes `IAuthManager` + `LocalDatabase` + `UserHttpClient`
  - ThirdpartyManager.ets — public constructor takes `ThirdpartyHttpClient`
  - UserManager.ets — public constructor takes `IAuthManager` + `UserHttpClient`
  - VoIPManager.ets — public constructor takes `VoipHttpClient`
- **Step 8**: Expanded ServiceRegistry.ets with all service register/get methods + `ConnectionConfig`
- **Step 9**: Rewrote AppNavigation.ets as composition root — creates all HTTP clients and services, registers them in ServiceRegistry
- **Step 10**: Updated all page files to replace `XxxManager.getInstance()` with `ServiceRegistry.getInstance().getXxxManager()`:
  - InviteManagementPage.ets — `MediaManager.getInstance()` → `ServiceRegistry.getInstance().getMediaManager()`
  - JoinRoomPage.ets — removed `MatrixHttpClient`, rewrote `searchUsers()` to use `RoomManager.searchUsers()`
  - JoinSpacePage.ets — removed `MatrixHttpClient`, rewrote `searchPublicSpaces()` to use `RoomManager.searchPublicRooms()`
  - RoomDetailPage.ets — removed singleton fields, uses ServiceRegistry for all managers, fixed `MessageManager` creation
  - SettingsPage.ets — all `.getInstance()` → `ServiceRegistry.getInstance().getXxxManager()`
  - UserProfilePage.ets — all `.getInstance()` → `ServiceRegistry.getInstance().getXxxManager()`
  - NotificationSettingsPage.ets — `PushManager.getInstance()` → `ServiceRegistry.getInstance().getPushManager()`
  - MainPage.ets — `UserManager.getInstance()` → `ServiceRegistry.getInstance().getUserManager()`
  - RoomListPage.ets — `RoomTagManager.getInstance()` → `ServiceRegistry.getInstance().getRoomTagManager()`
- **Step 11**: Updated component files:
  - MessageBubble.ets — `MediaManager.getInstance()` → `ServiceRegistry.getInstance().getMediaManager()`
  - ImageViewer.ets — same
  - FilePreview.ets — same
- **Step 12**: Fixed RoomDetailController.ets import paths (`./RoomManager` → `../services/RoomManager`, etc.)
- **Step 13**: Fixed AppNavigation.ets LocalDatabase import path (`../services/` → `../database/`)

### Remaining
- **Step 14**: Delete `MatrixHttpClient.ets` (or leave as dead code for now — no consumers remain)

### Key Decisions
- Simple singletons without MatrixHttpClient deps (MediaLibraryManager, ShareManager, DeviceSecurityManager, ScanManager, UserAuthManager, ServiceCollaborationManager, ThemeManager, VerificationService, CryptoService) remain as singletons — NOT registered in ServiceRegistry
- `IAuthManager` interface kept but `getHttpClient()` removed from it
- `getBaseUrl()` changed from `protected` to `public` in BaseHttpClient for MediaManager access
- `SyncManager` constructor takes 5 params: `IAuthManager, LocalDatabase, EventProcessor, CryptoService, BaseHttpClient`
- `RoomManager` constructor: `IAuthManager, LocalDatabase, RoomHttpClient`
- `MessageManager` constructor: `IAuthManager, LocalDatabase, MessageHttpClient`

## Critical Context
- MatrixHttpClient.ets is now dead code — no files import it except itself
- MatrixConfig is only referenced within MatrixHttpClient.ets
- All pages now use ServiceRegistry for the moved services
- Simple singletons (ScanManager, UserAuthManager, etc.) still use `.getInstance()` directly
